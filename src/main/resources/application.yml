spring:
  application:
    name: stp-client
  profiles:
    active: dev
  mvc:
    web:
      resources:
        add-mappings: false
server:
  port: 7003
  servlet:
    context-path: /stp-client
  error:
    include-message: always
logging:
  level:
    com.noh.stpclient.config.interceptor: DEBUG
